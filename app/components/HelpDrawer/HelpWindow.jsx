import * as React from "react"; import * as PropTypes from "prop-types";

const HelpWindow = class extends React.Component {
  render() {
    return (
      <div>
        Toggle
      </div>
    );
  }
}

export default HelpWindow;